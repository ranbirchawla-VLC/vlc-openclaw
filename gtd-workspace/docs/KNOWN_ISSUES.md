# GTD Workspace — Known Issues

Non-blocking carry-forwards from sub-step gates. Each entry lists priority and the target sub-step for resolution.

| ID | Description | Priority | Target |
|----|-------------|----------|--------|
| KI-001 | `_SCOPES` in calendar scripts includes both `calendar` and `calendar.events`; the latter is a subset of the former. Drop `calendar.events`. | P3 | next opportunity |
| KI-002 | `test_get_events.py` Case 5 has `main.__wrapped__ = None` as dead code with no effect. Remove. | P3 | next opportunity |
| KI-003 | `toToolResult` duplicated between `gtd-tools` and `nutriosv2-tools`. Extract to shared plugin-utils package before the third plugin lands (Sub-step 5 Gmail send). Decide pattern at Sub-step 5 prep. | P2 | before Sub-step 5 |
| KI-004 | `get_events.py`: `_now_iso()` / `_seven_days_iso()` called before span opens; span attributes set after. Move time computation inside span. | P3 | next opportunity |
| KI-005 | `calendar/conftest.py` `sys.path.insert` for the calendar directory is redundant; pytest auto-adds test dir. Remove. | P3 | next opportunity |
| KI-006 | `get_events.py` `_map_event`: no comment noting `htmlLink` to `html_link` camelCase-to-snake_case conversion. Add comment. | P3 | next opportunity |
| KI-007 | Flat 1-second retry sleep on Google API 5xx; Google recommends truncated exponential backoff. Fine for dev; required before sustained production calendar traffic. | P2 | before production traffic on calendar tools |
| KI-008 | `_CONTEXT_ENV` dict duplicated in both calendar scripts. Extract to `otel_common.py` when a third script needs it. | P3 | when third plugin tool lands |
| KI-009 | Plugin registration in root `openclaw.json` is an operator-side manual step. Standing risk across all agents; codify in deploy runbook. | P3 | runbook pass |
| KI-010 | `extract_parent_context` in `otel_common.py` is superseded by `attach_parent_trace_context` but retained because its own tests in `test_otel_common.py` still import it. Delete function + tests together. | P3 | Phase 8 carry-forwards |
| KI-011 | `test_otel_common.py` test 8 asserts `trace_id` match only; `parent_span_id` assertion delegated to integration test 11. Tighten test 8 to assert both. | P3 | Phase 8 carry-forwards |
| KI-012 | `make test-gtd-otel` narrow target does not include `gtd-workspace/scripts/tests/test_otel_phase1_integration.py`; narrow target gives incomplete coverage. | P3 | next Makefile tidy sub-step |
| KI-013 | `SPAWN_ENV` in `plugins/gtd-tools/otel-helpers.js` is a module-load-time snapshot (consistent with grailzee-eval-tools reference) but inconsistent with nutriosv2-tools call-time spreading. Decide canonical pattern; align all plugins. | P2 | portfolio-wide OTEL consistency pass |
| KI-014 | `AGENT_ARCHITECTURE.md` Pattern 2 still names the dispatcher attribute `capability_loaded`. The 2b.3 span contract corrected the semantic to `capability_dispatched` as boolean. Canon doc to follow. | P3 | portfolio-wide OTEL consistency pass (alongside KI-013) |
| KI-015 | Signal pattern false positives surfaced in Phase 2 Gate 2 review: `\bi need to\b` in capture fires before review signals ("I need to do my weekly review" routes to capture); `\bevents?\b` and `\bschedule\b` in calendar_read too broad ("can we schedule a call", "waiting on the events team" misroute). | P2 | dedicated signal pattern audit pass |
| KI-016 | `_load_api_key` duplicated verbatim in `turn_state.py` and `otel_common.py`. Two places to update on key path change. | P3 | portfolio-wide OTEL consistency pass (alongside KI-013, KI-014) |
| KI-017 | Test quality gaps in `test_turn_state.py` Phase 2 suite: test 19 (`test_latency_and_mtime_types`) is a subset of test 14 (`test_success_span_attributes`) with no distinct behavior; tests 7-8 (decision-6 edge cases) assert only `intent`, not `capability_prompt` — wrong file could be served silently. | P3 | next test cleanup pass |
| KI-018 | `AGENT_ARCHITECTURE.md` Pattern 1 originally named the dispatcher `turn_state` without flagging the flat-namespace constraint; second-agent collisions inevitable. Correction is the namespacing rule (per-agent unique name; convention `<agent>_dispatch`). Pattern crystallized at the Trina rename. | P3 | portfolio-wide canon pass |
| KI-019 | Handoff v3 referenced script path `skills/<trina-workspace>/scripts/capture_gtd.py`; actual path is `gtd-workspace/scripts/gtd/capture.py`. Future handoffs must cite verified paths. | P3 | handoff discipline pass |
| KI-020 | Handoff v3 claimed `capture.py` envelope contract was LLM-populated; diagnostic confirmed gateway-injected via `OPENCLAW_CHANNEL_TYPE` and `OPENCLAW_CHANNEL_PEER_ID`. Future handoffs must cite line numbers for substantive code claims or flag as conjecture. | P3 | handoff discipline pass |
| KI-021 | Canon Pattern 7 references `DATA_ROOT`; gtd workspace uses `GTD_STORAGE_ROOT`. Either normalize or document per-agent override convention in canon. | P2 | 2c design phase |
| KI-022 | Path segment in gtd workspace is `gtd-agent/users/<user_id>/`; Pattern 7 specifies `<agent>/<user_id>/`. Reconcile with canon during 2c restructure. | P2 | 2c design phase |
| KI-023 | gtd workspace has two test trees; `make test-gtd` covers `gtd-workspace/scripts` only and ignores `gtd-workspace/tests` legacy tree. Consolidate or scope explicitly. | P3 | test infra cleanup pass |
| KI-024 | Code rationalized scope expansion citing prior context as authoritative ("consistency consequence," "logical follow-on," "pre-existing audit"). Bounded-autonomy clauses must explicitly disclaim prior authorization. | P2 | supervisor-session skill update |
| KI-025 | (N-1) Pattern 7 path convention mismatch documented in `TOOLS.md` without flagging the deviation from canon. | P2 | 2c design phase (fold into KI-022) |
| KI-026 | (N-2) `TOOLS.md` Paths block omits the default `storage_root` fallback documented in `tools/common.py`; operator cannot infer unset-env behavior. | P3 | next opportunity |
| KI-027 | (N-3) `test_capture_user_pathing.py` does not assert `record_type` absence from the projection; primary failure mode is still covered. | P3 | next opportunity |
| KI-028 | (2b.3 Gate 2 N-3) Six of seven capability files state composition guardrails correctly but do not annotate the user or tech outcome each guardrail traces to. Only `unknown.md` guardrail 1 cites "tech outcome 4." Behavior is correct; auditability annotation is missing across `capture`, `query_tasks`, `query_ideas`, `query_parking_lot`, `review`, `calendar_read`. Fix: add "(traces to user outcome N)" or "(traces to tech outcome N)" reference to each guardrail. | P3 | next sub-step that touches capability files |
| KI-029 | `conftest.py` files across `gtd-workspace/scripts/` and subdirectories use `sys.path.insert` for test discovery. Pattern is fragile when new test directories are added outside existing `sys.path` insertion points. Resolution: consolidate path setup to a single top-level `conftest.py` or adopt `pyproject.toml` `pythonpath` setting. Shared script tests currently work around this via `importlib.util.spec_from_file_location`. | P3 | next sub-step touching test infrastructure |
| KI-030 | No API timeout or overload handling in the GTD agent surface. When Anthropic returns `overloaded_error` or `api_error` mid-session, `trina_dispatch` (and any inner LLM call in `turn_state.py`) exits non-zero and the PREFLIGHT silently fails. The agent recovers conversationally but the capability routing is bypassed for that turn. Needs: (1) retry with backoff in `turn_state.py` for transient API errors distinct from parse/validation failures; (2) user-visible signal when PREFLIGHT fails so the operator knows routing was skipped rather than inferring it from the audit log. | P2 | sub-step 2e or dedicated reliability pass |
