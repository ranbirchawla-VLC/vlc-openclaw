# 2b.3 Build Phase Handoff

**Date:** 2026-05-04
**Phase:** Build, sub-step 2b.3
**Branch:** `feature/sub-step-2b3-capability-wiring` (off main at `ea4a894`)
**Last commit:** `5d8e5e0`

---

## What's locked

All design phase artifacts plus all Phase 1 supervisor decisions. No reopening without explicit "unlock" plus fresh decision-reasoning doc.

**Phase-gated commits, Gate 2 per phase.** Refinement of the build sequence framing decided 2026-05-04 mid-Phase-1. Each phase: Gate 1 (tests green) → pre-review commit → Gate 2 (code-reviewer subagent fresh context) → post-review commit → next phase. Gate 3 (Telegram round-trip with Honeycomb verification) lands once at sub-step close against the integrated surface; per-phase Gate 3 is not meaningful for surfaces with no user-visible behavior in isolation.

**Phase 1 supervisor-locked answers (binding for remainder of 2b.3):**

1. JS test runner: `node:test` built-in plus `@opentelemetry/sdk-trace-base` devDependency.
2. error.code vocabulary at the plugin layer: `subprocess_spawn_error`, `subprocess_nonzero_exit`, `output_parse_failure`. Extension at later sub-steps if a fourth branch surfaces.
3. Integration test harness: in-process fallback (patched sys.argv; in-process InMemorySpanExporter) authorized when subprocess spawn capture fights the harness. Cross-process parity verified at Gate 3 step 1 in Honeycomb, not in unit tests.
4. `OTEL_EXPORTER_OTLP_PROTOCOL` forwarded in SPAWN_ENV.
5. `attach_parent_trace_context` lives in `gtd-workspace/scripts/otel_common.py` (not `common.py`); GTD has consolidated OTEL utilities there.
6. `OTEL_SERVICE_NAME` value: `gtd-tools` (plugin id verbatim; matches grailzee-eval convention).
7. delegation.py excluded from any Phase 1-7 work; Phase 6 deletes it.
8. `extract_parent_context` retained until Phase 8 cleanup (KI-010); has 5 callers in tests but zero in production scripts.

**Reference implementation pointer.** `plugins/grailzee-eval-tools/index.js` and `skills/grailzee-eval/scripts/grailzee_common.py` are the canonical OTEL pattern 2 reference for the rest of 2b.3. Code studied them in Phase 1; pattern absorbed.

---

## What's in flight

Nothing. Phase 1 closed cleanly at commit `5d8e5e0`.

---

## What's queued

Phase 2: `turn_state` plugin and dispatcher. The architectural anchor of 2b.3.

Per build sequence Phase 2:

1. Author `scripts/turn_state.py`:
   - Python signal + keyword classifier (deterministic first pass per classifier spec v1 decision 5; high-precision philosophy).
   - Inner LLM classifier fallback (structured JSON return; bounded vocabulary; temp=0; pinned to `mnemo/claude-sonnet-4-6`; bypasses mnemo proxy per pattern note in agent_api_integration_pattern.md).
   - Capability file fresh-read per turn (no caching; `capability_file_mtime` captured for span).
   - Returns `{intent, capability_prompt}`.
   - Span attributes per span contract v1: `intent`, `capability_dispatched`, `capability_file`, `classifier_strategy`, `classifier_latency_ms`, `capability_file_mtime`, `continuity_turn`.
   - Error path per error span discipline (status ERROR; bounded `error.code`; no PII; no exception.message; no stacktrace).
2. Author Python signal patterns per intent (high precision; false positive worse than false negative).
3. Author inner LLM classifier prompt (~40-60 lines; correct/incorrect pattern from `deal.md`; bounded vocabulary; explicit prohibition on parameter extraction).
4. Register `turn_state` in `plugins/gtd-tools/tool-schemas.js`.
5. Add `turn_state` to `tools.allow` in agent entry.
6. Tests (Gate 1):
   - Per-vocabulary classification at temp=0 3x require-all-pass.
   - Continuity-turn handling (single-word continuations after clarification questions).
   - LLM fallback on signal miss.
   - Edge case routing per classifier spec v1 decision 6.
   - Span emission tests for all attributes.

**Phase 2 will introduce the first LLM tests in the GTD workspace.** `make test-gtd-llm` target wires up here; CLAUDE.md gate extension applies (model pinned, temperature=0, 3x require-all-pass, fixtures match production).

**Phase 2 needs an `error.code` vocabulary decision before tests author.** Classifier and dispatcher surface errors are not in the locked Phase 1 vocabulary; build-time decision per build sequence Phase 2.

---

## Carry-forwards

KNOWN_ISSUES.md entries logged in Phase 1, all targeting later sub-steps:

- KI-010: `extract_parent_context` retained pending test cleanup; target Phase 8.
- KI-011: `test_otel_common.py` test 8 asserts trace_id only; parent_span_id covered by test 11; tighten test 8. P3; target Phase 8.
- KI-012: `make test-gtd-otel` narrow target excludes `test_otel_phase1_integration.py`. P3; next Makefile tidy.
- KI-013: SPAWN_ENV module-load-time snapshot consistent with grailzee-eval but inconsistent with nutriosv2-tools. P2; target portfolio-wide OTEL consistency pass.

Z3 carry-forwards still pending for Phase 8 opportunistic fold-in: N-1, N-2, N-3, N-5, N-6, N-7, O-2.

---

## What the next session reads first

In order:

1. `CLAUDE.md` (project knowledge)
2. `AGENT_ARCHITECTURE.md` (project knowledge)
3. `agent_api_integration_pattern.md` (project knowledge)
4. This handoff document
5. `2b3-build-sequence-2026-05-04-v1.md` (Phase 2 detail)
6. `2b3-decision-reasoning-2026-05-04-v1.md` (Layer 3.1 classifier spec; Layer 3.3 span contract)
7. `2b3-soul-anchor-2026-05-04-v2.md` (voice register; relevant when Phase 2 LLM classifier prompt drafts)
8. `2b3-outcomes-lock-2026-05-04-v1.md` (tech outcome 3 dispatch determinism; tech outcome 4 negative-path safety; tech outcome 5 capability hot-reload; tech outcome 6 span attribute coverage)

Optional but useful: `2b3-success-criteria-cadence-2026-05-04-v1.md` (Gate 3 verification target).

---

## State of the world

- Branch `feature/sub-step-2b3-capability-wiring` at commit `5d8e5e0`.
- Two commits on the branch; Phase 1 pre-review (`4f01fce`) and Phase 1 post-review (`5d8e5e0`).
- main at `ea4a894`.
- 9/9 JS tests green; 187/187 Python tests green.
- Trina's plugin and Python script layers carry full OTEL Pattern 2 plumbing. Honeycomb traces will be parent-child connected from the gateway through plugin spans through Python child spans the moment turn_state lands and gets called.
- `~/.openclaw/agents/gtd/agent/auth-profiles.json` exists.
- `package-lock.json` untracked at session close. Operator decision pending (commit alongside Phase 2 work, or .gitignore deliberately).

---

_Phase 1 closed. Phase 2 opens against this state._
