[pytest]
# Restrict test discovery to skills/ so bare pytest only collects skill test suites.
testpaths = skills
markers =
    llm: slow LLM integration tests — require Anthropic API access (run via make test-llm)
