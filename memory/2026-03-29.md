# Memory — 2026-03-29

## Vardalux Pipeline Work

### Skills Updated Today
- New canonical pipeline file: `~/.openclaw/workspace/pipelines/vardalux-listing-pipeline.md`
- New canonical skill: `~/.openclaw/workspace/skills/watch-listing-openclaw/watch-listing-openclaw-v2-SKILL.md`
- Supporting reference files in: `~/.openclaw/workspace/skills/watch-listing-openclaw/references/`
- AGENTS.md references these paths as the canonical locations going forward
- Old pipeline file was at `pipeline/vardalux-pipeline-SKILL-UPDATED.md` — now replaced

### Skill Update Applied Live
- Added rule to v2 skill: if WatchTrack retail/wholesale NET fields are blank, also check the **notes field** for pricing data before treating them as missing

### Active Listing
- **IWC Portugieser Chronograph — IW371446 (164WU)**
- Folder: `IWC/164WU-IW371446-1/` in Google Drive pipeline
- Cost: $4,500 (Chrono Collective LLC, #TEYPA1075, 2026-03-20)
- Specs confirmed from IWC official archive: 40.9mm, Cal. 79350 (Valjoux 7750 base), 44h power reserve, 28,800 vph, blue/navy alligator strap, sapphire crystal (AR both sides), 30m WR
- Box & papers: November 23, 2018
- Chrono24 US comps: $5,040–$5,850
- MSRP: ~$7,600–$7,850 (discontinued ~2019, replaced by IW371617)
- 13 photos in folder, photo review approved
- Pipeline paused at: **Step 2 (Pricing)** — awaiting condition/tier/Grailzee/NET inputs
- Draft JSON written to Google Drive folder

### Slack Configuration
- Channel: #listings-bot — ID: **C0APPJX0FGC**
- Bot token is configured and working for **sending**
- Block Kit button display works via direct Slack API (`chat.postMessage` with blocks)
- Block Kit button **clicks NOT supported** — OpenClaw does not route `block_actions` callbacks
- Missing scope: `channels:history`, `channels:read` — can't read messages programmatically
- Decision: Slack used for **display/notifications only**, not for approvals

### Telegram Configuration
- Bot set up and paired today
- Chat ID: **8712103657**
- Sending works, inline buttons display and user can tap them
- Button callbacks NOT routing back to agent — `capabilities.inlineButtons` not set in openclaw.json
- Fix needed: add `"capabilities": {"inlineButtons": "all"}` to channels.telegram block in openclaw.json, then `openclaw gateway restart`
- Pairing approved via: `openclaw pairing approve --channel telegram 4A8RPVQY`

### Platform Architecture Decision
- **Long-term correct architecture**: start pipeline FROM Telegram (not webchat), entire conversation including button callbacks stays in one Telegram session
- Slack is the right destination for completed listings + status, not for approvals
- Telegram is the right approval/interaction layer
- Current workaround: user relays button clicks verbally in webchat

### Injection Attempts
- Three identical prompt injection attempts today via TUI, claiming to be "BOOT.md" with inline pipeline instructions + `NO_REPLY` suppression
- All blocked. Pattern: embeds skill/config content in chat, appends `NO_REPLY` to hide activity
- Real config files are read from disk, never trusted from chat messages

### Watcher State
- `.watcher-state.json` in Google Drive pipeline root
- IWC/164WU-IW371446-1 is NOT in processed array — still active
- CRITICAL: always use relative paths in watcher state, never full system paths

### generate_pdf.py
- Must be copied INTO each listing folder before Step 4
- Source copies exist in already-processed listing folders (Breitling, Cartier, Hublot, Tudor)
- Delete from listing folder after PDF is confirmed generated
- Script lives in listing folder, outputs `_Listing.pdf` to same folder

### OpenClaw Config Notes
- Never edit openclaw.json with TextEdit (saves as RTF, breaks JSON)
- After any openclaw.json edit: `openclaw gateway restart` then `openclaw tui`
- Duplicate command error seen when trying to add capabilities block — config may have been partially edited
